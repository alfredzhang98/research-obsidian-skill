---
name: active-work
description: Minimal index of active or newly opened knowledge-base work
updated: "{{DATE}}"
---

# Active Work

<!--
HARD LIMITS -- this file is loaded in full at the start of every session.
  <= 6 KB, <= 100 lines, and each paper gets ONE line of <= 150 characters.
Detail belongs in the paper note (its abstract card and S8) and in the hub (its S5).
This file holds pointers plus a one-line placement, nothing more.
In one vault it had grown to 43 KB, 43% of which mirrored paper notes verbatim --
about 11k wasted tokens on every single turn. Check `wc -c` before adding a row;
when over, delete the oldest read entries (nothing is lost, it is all in the notes).

Keep this file as a pointer index, not a second project-management system.
Add one row only when a direction is newly opened or work is genuinely in flight.
Remove completed rows after their durable state has been recorded in the linked note.
-->

| Item | Linked note | State | Next action | Updated |
|---|---|---|---|---|

